import HomeComponent from "@component/HomeComponent.vue";

const routes = [
    {
        path: '/',
        component: HomeComponent,
        meta: {title: 'QUEUE'}
    },
];

export default routes;
